## Running the code
  Run `npm i` to install the dependencies.
  Run `npm run stub-server` to start the Stubbing server (Optional, only if Legal-OCR Backed not available).
  Run `npm run dev` to start the development server.
  